# top-most EditorConfig file
root = true

# Unix-style newlines with a newline ending every file
[*]
end_of_line = lf
insert_final_newline = true

# Why would ever not use utf8?
charset = utf-8

# That how Jon likes it!
indent_style = tab
indent_size = 2
trim_trailing_whitespace = true